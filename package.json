{
  "name": "private-notify",
  "version": "0.3.2",
  "private": true,
  "description": "Encrypted native Android notifications with a Go CLI and optional FCM relay.",
  "scripts": {
    "android:build": "android\\gradlew.bat -p android assembleDebug",
    "android:install": "android\\gradlew.bat -p android installDebug",
    "android:release": "android\\gradlew.bat -p android assembleRelease"
  }
}
